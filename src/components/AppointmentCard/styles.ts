import styled from 'styled-components'

import { AppointmentStatusEnum } from './types'

export const AppointmentCardWrapper = styled.div<{
  status: AppointmentStatusEnum
}>``

export const AppointmentCardTitle = styled.h2``

export const AppointmentCardDescription = styled.p``
